# 项目档案 · Case Files

> 这个文件是网站「查看项目档案」内容的**唯一来源**。直接修改下面的文字即可更新网站，无需改代码。
>
> **格式规则**（请保持）：
> - `## 编号 项目名` 开启一个项目（编号用 01–05，要和网站里的项目对应）。
> - `### 小节名` 开启一个小节。小节名请保持不变，顺序可调：
>   问题 / 背景 / 用户 / 工作流程 / 演示 / 技术方案 / Prompt 设计 / 评估方法 / 反思。
> - 小节正文可以多行；空一行分段。
> - 标了「（待补充）」的地方是需要你补充真实内容的部分。

---

## 01 灵活规划

### 问题
计划不是一次生成，而是持续调整。

绝大多数日程工具默认「用户知道今天要做什么」，因此更像一个记录工具。现实却不是这样——一天中不断出现临时会议、插队的新任务、精力下降、完成速度变化、Deadline 改变。

用户真正花费大量时间的，不是制定计划，而是不断修改计划。传统 Calendar 和 Todo List 无法理解这些变化，只能依赖用户自己重新安排。结果就是：今天的计划越来越乱、已完成任务没人总结、未完成任务没人递补、用户越来越放弃维护计划。

因此，本项目希望解决：让 AI 持续维护计划，而不是一次性生成计划。

### 背景
目前主流产品（Google Calendar、Notion、Todoist、TickTick、Motion、Reclaim AI）主要做记录任务、同步日历、提醒、自动找空闲时间。

但它们很少真正理解：用户为什么改计划、哪些任务应该继续、哪些应该放弃、哪些已经完成一半。AI 更多停留在「帮你生成一个初始计划」，而不是「成为持续工作的 Schedule Agent」。

所以本项目探索的是：AI 如何持续管理一个不断变化的计划。

### 用户
目标用户：知识工作者、研究生、程序员、AI 学习者、自由职业者。

共同特点：每天需要推进多个长期目标、大量任务没有固定截止时间、计划经常变化、容易因为一次打断导致当天计划全部失效。

典型场景：「我今天本来准备学习 LangGraph，结果老板临时开会，下午又收到新的作业，晚上已经没有精力了——现在所有计划都失效了。」

### 工作流程
整个系统采用持续循环，而不是一次规划：

用户输入目标 → 意图理解 → 任务拆解 → 时间规划 → 生成今日计划 →〔执行 → 用户反馈 → 状态更新 → 重新规划 → 计划更新〕循环 → 每日复盘 → 学习用户节奏 → 优化下一次规划。

整个过程中用户无需重新创建计划。Agent 负责：判断意图、维护状态、重新安排、更新完成情况、生成总结。

### 技术方案
架构：React → FastAPI → AI Orchestrator → LangGraph → 节点（Intent → Task → Schedule → Reply）→ PostgreSQL。

技术栈：React、FastAPI、Python、LangGraph、OpenAI API、PostgreSQL、Pydantic。

为什么采用 LangGraph：整个系统不是一次推理，而是持续维护状态。LangGraph 可以保存 State、支持条件跳转、支持 Human in the Loop、支持多节点协作，未来方便加入 Memory / Review Agent / Calendar Agent / Notification Agent。

### Prompt 设计
不同节点采用不同 Prompt：

· Intent：输入用户消息、当前计划、等待确认任务，输出意图类型（create_plan / add_task / modify_schedule / feedback / delete / query），避免依赖大量正则。

· Task Decomposer：输入自然语言目标、Deadline、学习资料，输出任务列表、预计耗时、依赖关系。

· Scheduler：输入任务、空闲时间、优先级、用户偏好，输出时间安排；同时要求避免重叠、避免连续过长学习、考虑 Deadline。

· Reply：不直接返回 JSON，而是用自然语言解释「为什么这样安排」（例如：今天精力有限，因此先完成论文，再继续课程学习），提升可解释性。

### 评估方法
最初采用 Unit Test，主要验证：没有时间重叠、新增任务正确插入、重排后无重复。但实践发现——全部测试通过，真实产品依旧存在重复任务、状态错误、时间冲突、Intent 判断错误。

因此重新设计 Eval：① API Test（模拟真实接口）② Browser Test（模拟真实用户）③ Human Test（人工完成典型流程）④ LLM Judge（让 GPT 判断计划是否合理并给出评分）。

评价指标：计划完成率、重排次数、用户修改次数、时间冲突率、重复任务率、用户满意度。

### 反思
最大的收获：最初完全依赖 LLM，结果模型容易忘记状态、重复生成、修改错误。于是转向任务拆解 + 时间规划 + 状态管理的模块化设计。进一步学习 LangGraph 后发现：Agent 的重点不是调用更多模型，而是设计稳定的状态流。

另一个重要发现：传统 Unit Test 很容易全部通过，却无法覆盖真实用户行为。因此后续更重要的是建立真实的端到端验收（E2E Evaluation）。

**Future Work**
① 学习用户节奏：根据历史数据自动学习不同类型任务的真实耗时、不同时间段的效率、拖延模式，用于动态估计时间。
② Memory：长期记录学习速度、兴趣方向、常见打断，形成长期画像。
③ 多 Agent：拆分 Planner / Reviewer / Memory / Calendar / Notification，多个 Agent 协同完成规划。
④ Calendar Integration：同步 Google / Apple / Outlook，真正管理用户日程。

**项目成果**
· 基于自然语言生成动态七日学习计划。
· 支持新增、修改、删除、反馈等多种任务管理操作。
· 引入 LangGraph 构建状态驱动的 Agent 工作流，实现可扩展的任务规划架构。
· 从仅依赖单元测试演进为 API + 浏览器 + 人工验收相结合的评测体系，提升真实场景下的可靠性。
· 完成从规则驱动到 LLM 协同决策的架构迭代，为后续接入 Memory、个性化时间预测和多 Agent 协作奠定基础。

---

## 02 AI 知识回顾助手

### 问题
收藏夹是「信息坟场」：你保存了大量文章、链接、想法，但几乎从不回看，因为找不到、也想不起来存过什么。

### 背景
传统书签按文件夹和标题组织，检索靠「你还记得关键词」。当收藏量变大，人工整理的成本超过收益，于是收藏 = 遗忘。

### 用户
信息摄入量大、需要把读过的东西真正复用的人：研究者、产品、内容创作者。

### 工作流程
通过 Telegram Bot 一键保存内容 → 自动抓取正文、生成摘要、打标签、向量化入库 → 之后用自然语言提问检索（如「上次那篇讲 Agent 记忆的笔记」）→ 返回最相关的条目。

### 技术方案
Python；用 Embeddings 做语义向量；Qdrant 做向量检索；Telegram Bot 作为轻量收集入口。

### Prompt 设计
（草稿）保存时用提示生成结构化摘要 + 标签；检索时把自然语言查询改写为语义检索意图。（待补充：真实提示词）

### 评估方法
（待补充：检索命中率 / 回看率 / 主观「找得到」的感受）

### 反思
（待补充）

---

## 03 随手做菜

### 问题
年轻人每天都要回答「今天吃什么」——看似简单，实际很耗决策精力：菜谱散落各处、选好菜才发现缺料、冰箱食材放到过期被浪费。

一句话概括：一个帮用户用现有食材快速决定今天吃什么、顺便减少浪费的微信小程序。

### 背景
市面上的同类产品多是「菜谱大全」，本质是内容消费；而真实的高频场景其实是——「用手头食材快速做出一顿饭」。

所以我把定位明确为：不是菜谱库，而是库存驱动的轻量决策工具——这也是它和市面产品最大的差异。

（个人项目，AI 辅助 ｜ 2026 ｜ 微信小程序 + 云开发。独立完成从用户与场景分析 → PRD → 功能优先级 → 产品 / 交互设计 → 数据埋点与验证方案的全流程，开发环节借助 Claude / Cursor 实现。）

### 用户
需要自己张罗一日三餐、又想少花决策精力、少浪费食材的年轻人 / 小家庭。他们的痛点不是「找不到菜谱」，而是「面对现有食材，懒得想今天做什么」。

### 工作流程
核心闭环：录入食材 → 基于库存推荐 → 选菜做饭 → 做完推断扣减库存 → 库存变化再进入推荐。

关键产品决策——为什么优先做「做完菜扣减库存」？整个产品成立的前提是「用户愿意持续维护库存」，这也是最大风险。而「做饭 → 消耗」这一环如果缺失，库存数据会很快失真，推荐和临期提醒都会失效。所以把它排为 P0：用一件事盘活整个闭环，而不是先堆更多推荐功能。

### 技术方案
微信原生前端 + 云开发（云函数 / 云数据库）后端，个人独立落地（借助 Claude / Cursor 提升交付效率）。

AI 在产品侧解决真实体验问题：

· OCR 截图识别入库：接入腾讯云 OCR，把购物订单截图一键识别成食材；自研后处理过滤价格 / 店名 / 广告噪声，提取「菜名 + 重量」，再用食材词库校验，只保留真正的食材。

· 库存匹配 + 加权推荐：按食材重要性分级判断「能不能做」，再结合「临期优先、荤素搭配」动态加权，让「今天抽一顿 / 生成菜单」贴合真实库存。

### Prompt 设计
本项目的 AI 主要用在 OCR 识别与库存加权推荐（自研规则 + 词库校验），核心链路暂不依赖 LLM 提示词；AI 编程（Claude / Cursor）则用于提升独立开发的交付效率。

### 评估方法
自研轻量埋点（事件写入云数据库）+ 云函数计算指标，围绕关键假设设计指标体系：

· 用户是否知道下一步：首个动作转化、空转退出率

· 库存是否核心、是否愿意维护：库存维护率、维护 vs 未维护用户的推荐接受率对比

· 推荐是否有价值：推荐接受率、按匹配度的接受率

· 核心闭环是否跑通：做完菜扣库存率

目的是用数据验证假设、决定开发优先级，而不是凭感觉堆功能。

### 反思
完成了从需求到可体验版本的 0→1，跑通「库存驱动决策 + 社交化分享点单」的核心链路。

局限：个人项目、暂无规模化用户，指标体系用于后续灰度验证。下一步：基于埋点数据决定是否强化偏好推荐，补充做菜记录、不重复推荐等。
