# 项目档案 · Case Files

> 这个文件是网站「查看项目档案」浮层内容的**唯一来源**（精简概览版；完整案例见各项目的完整档案页）。直接修改下面的文字即可更新网站，无需改代码。
>
> **格式规则**（请保持）：
> - `## 编号 项目名` 开启一个项目（编号 01–03，要和网站里的项目对应）。
> - `### 小节名` 开启一个小节：问题 / 背景 / 用户 / 工作流程 / 技术方案 / Prompt 设计 / 评估方法 / 反思。
> - 小节正文可多行；空一行分段。

---

## 01 灵活规划（Replan）

### 问题
传统日程工具的问题不是记录，而是计划生成之后，世界就变了。真实痛点：有个目标（写论文 / 学课程），却不知道怎么拆、每天做什么、做不完怎么调整。

### 背景
目标不是「做个日历」，而是做一个能理解任务、安排时间、并在进度变化后自动重排的系统。定位：不是聊天机器人，是围绕「任务状态」运转的规划系统——每次输入都改变当前计划。

### 用户
学生 / 个人效率场景：有多个长期目标、任务没有固定截止时间、计划经常变化、容易因一次打断就当天计划全失效的人。

### 工作流程
目标交互路径是：一句话说个目标 → 拆成可执行子任务 → 按 deadline / 作息排期 → 根据反馈重排。当前各模块已经分别实现，但跨模块状态同步和反馈后的稳定重排仍在联调。

### 技术方案
关键是分层：LLM 处理语义理解（意图、语义拆解），确定性模块处理排期、冲突检测与状态更新。React/Vite + Python 后端 + LLM + 本地规则兜底。

### Prompt 设计
意图收敛成 6 个纯语义意图 + 状态机解析（LLM 判语义、状态定动作）；拆解用 few-shot + 抽象↔具体对照。

### 评估方法
发现原本「全绿」的评测和真实链路脱节，于是开始重建端到端场景、质量评测与覆盖矩阵。当前评测可以帮助暴露问题，但尚未形成稳定的端到端验收基线。

### 当前状态
核心模块已分别实现，并完成部分场景测试；但真实端到端交互中仍存在状态不同步、任务冲突与反馈后重排偏差，正在收束最小稳定闭环。

### 反思
该项目尚未形成稳定上线版本，但通过实际开发暴露了 Agent 系统中「局部模块通过不等于端到端可用」的问题，也促使我重新设计评测、验收与变更管理流程。

---

## 02 书签助手

### 问题
收藏的瞬间，遗忘就开始了。收藏夹是「只进不出」的信息垃圾填埋场——要解决的不是「存不下」，而是「存了取不回、也从不回看」。

### 背景
一次产品重定义：核心价值从「更好的文件夹（整理）」改成「主动帮你重新想起（回顾）」，引入间隔重复式的「今日回顾」作为核心，分类与搜索退为支撑。

### 用户
在小红书 / 知乎 / 掘金等各平台收藏「想稍后再看」、但绝大多数在保存那一刻就被遗忘的人。

### 工作流程
一键保存（Chrome 扩展直接读现有书签，零迁移）→ 自动分类 / 聚类 / 摘要 / 向量化 → 语义搜索 + RAG 问答 + 相似推荐 →「今日回顾」把遗忘的收藏推回来。我关心的不是收纳，是「再次被用上」。

### 技术方案
后端做大脑、客户端做薄采集；SQLite 四表把「原始页面 / AI 衍生 / 用户行为 / 向量」分离，迭代 AI 时不丢用户状态。Chrome 扩展 + Python + LLM + BGE-M3（本地）。

### Prompt 设计
LLM 按 15 类固定 taxonomy 分类替代关键词表；AI 不可用时降级本地规则，仍返回真实分类而非全「Other」。

### 评估方法
「分类全是 Other」时先建标注评测集量化归因，定位到真凶是配置 bug 而非模型——本地分类器 57%→96%、LLM 98.2%；再用逐类 F1 修复类别回归，全程不 train-on-test。

### 反思
两条判断落进产品决策：「AI 是增强，不是地基」「先量化、先归因，再动手」。完整演进、架构与指标见完整档案。

---

## 03 随手做菜

### 问题
年轻人每天都要回答「今天吃什么」——看似简单，实际很耗决策精力：菜谱散落各处、选好菜才发现缺料、冰箱食材放到过期被浪费。

一句话概括：一个帮用户用现有食材快速决定今天吃什么、顺便减少浪费的微信小程序。

### 背景
市面上的同类产品多是「菜谱大全」，本质是内容消费；而真实的高频场景其实是——「用手头食材快速做出一顿饭」。

所以我把定位明确为：不是菜谱库，而是库存驱动的轻量决策工具——这也是它和市面产品最大的差异。

（个人项目，AI 辅助 ｜ 2026 ｜ 微信小程序 + 云开发。独立完成从用户与场景分析 → PRD → 功能优先级 → 产品 / 交互设计 → 数据埋点与验证方案的全流程，开发环节借助 Claude / Cursor 实现。）

### 用户
需要自己张罗一日三餐、又想少花决策精力、少浪费食材的年轻人 / 小家庭。他们的痛点不是「找不到菜谱」，而是「面对现有食材，懒得想今天做什么」。

### 工作流程
核心闭环：录入食材 → 基于库存推荐 → 选菜做饭 → 做完推断扣减库存 → 库存变化再进入推荐。

关键产品决策——为什么优先做「做完菜扣减库存」？整个产品成立的前提是「用户愿意持续维护库存」，这也是最大风险。而「做饭 → 消耗」这一环如果缺失，库存数据会很快失真，推荐和临期提醒都会失效。所以把它排为 P0：用一件事盘活整个闭环，而不是先堆更多推荐功能。

### 技术方案
微信原生前端 + 云开发（云函数 / 云数据库）后端，个人独立落地（借助 Claude / Cursor 提升交付效率）。

AI 在产品侧解决真实体验问题：

· OCR 截图识别入库：接入腾讯云 OCR，把购物订单截图一键识别成食材；自研后处理过滤价格 / 店名 / 广告噪声，提取「菜名 + 重量」，再用食材词库校验，只保留真正的食材。

· 库存匹配 + 加权推荐：按食材重要性分级判断「能不能做」，再结合「临期优先、荤素搭配」动态加权，让「今天抽一顿 / 生成菜单」贴合真实库存。

### Prompt 设计
本项目的 AI 主要用在 OCR 识别与库存加权推荐（自研规则 + 词库校验），核心链路暂不依赖 LLM 提示词；AI 编程（Claude / Cursor）则用于提升独立开发的交付效率。

### 评估方法
自研轻量埋点（事件写入云数据库）+ 云函数计算指标，围绕关键假设设计指标体系：

· 用户是否知道下一步：首个动作转化、空转退出率

· 库存是否核心、是否愿意维护：库存维护率、维护 vs 未维护用户的推荐接受率对比

· 推荐是否有价值：推荐接受率、按匹配度的接受率

· 核心闭环是否跑通：做完菜扣库存率

目的是用数据验证假设、决定开发优先级，而不是凭感觉堆功能。

### 反思
完成了从需求到可体验版本的 0→1，跑通「库存驱动决策 + 社交化分享点单」的核心链路，目前朋友内测中。

局限：个人项目、暂无规模化用户，指标体系用于后续灰度验证。下一步：基于埋点数据决定是否强化偏好推荐，补充做菜记录、不重复推荐等。
